<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.researchi.admin.legacy.application.mapper.LegacyApplicationConsentMapper">

    <resultMap id="consentResultMap" type="com.researchi.admin.legacy.application.domain.LegacyApplicationConsent">
        <id property="id" column="id"/>
        <result property="researchNo" column="research_no"/>
        <result property="researchAppSeq" column="research_app_seq"/>
        <result property="requiredPrivacyYn" column="required_privacy_yn"/>
        <result property="futureRecruitmentYn" column="future_recruitment_yn"/>
        <result property="smsYn" column="sms_yn"/>
        <result property="emailYn" column="email_yn"/>
        <result property="consentVersion" column="consent_version"/>
        <result property="noticeSnapshot" column="notice_snapshot"/>
        <result property="consentedAt" column="consented_at"/>
        <result property="futureConsentExpiresAt" column="future_consent_expires_at"/>
        <result property="withdrawnAt" column="withdrawn_at"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.researchi.admin.legacy.application.domain.LegacyApplicationConsent" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin_legacy_application_consent (
            research_no, research_app_seq, required_privacy_yn,
            future_recruitment_yn, sms_yn, email_yn,
            consent_version, notice_snapshot, consented_at,
            future_consent_expires_at, withdrawn_at, created_at
        ) VALUES (
            #{researchNo}, #{researchAppSeq}, #{requiredPrivacyYn},
            #{futureRecruitmentYn}, #{smsYn}, #{emailYn},
            #{consentVersion}, #{noticeSnapshot}, #{consentedAt},
            #{futureConsentExpiresAt}, #{withdrawnAt}, CURRENT_TIMESTAMP
        )
    </insert>

    <select id="findByApplication" resultMap="consentResultMap">
        SELECT *
        FROM admin_legacy_application_consent
        WHERE research_no = #{researchNo}
          AND research_app_seq = #{researchAppSeq}
        LIMIT 1
    </select>

    <select id="findActiveFutureRecruitmentApplicationSeqs" resultType="java.lang.Long">
        SELECT research_app_seq
        FROM admin_legacy_application_consent
        WHERE research_app_seq IN
        <foreach collection="researchAppSeqs" item="researchAppSeq" open="(" separator="," close=")">
            #{researchAppSeq}
        </foreach>
          AND future_recruitment_yn = 'Y'
          AND withdrawn_at IS NULL
          AND future_consent_expires_at &gt; #{now}
          AND (sms_yn = 'Y' OR email_yn = 'Y')
    </select>
</mapper>
