<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.researchi.admin.matching.mapper.AdminKeywordMatchJobMapper">

    <resultMap id="adminKeywordMatchJobResultMap" type="com.researchi.admin.matching.domain.AdminKeywordMatchJob">
        <id property="id" column="id"/>
        <result property="documentSrl" column="document_srl"/>
        <result property="matchStatus" column="match_status"/>
        <result property="matchedCount" column="matched_count"/>
        <result property="createdAt" column="created_at"/>
        <result property="completedAt" column="completed_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.researchi.admin.matching.domain.AdminKeywordMatchJob" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin_keyword_match_job (
            document_srl,
            match_status,
            matched_count,
            completed_at
        ) VALUES (
            #{documentSrl},
            #{matchStatus},
            #{matchedCount},
            #{completedAt}
        )
    </insert>

    <update id="update" parameterType="com.researchi.admin.matching.domain.AdminKeywordMatchJob">
        UPDATE admin_keyword_match_job
        SET
            match_status = #{matchStatus},
            matched_count = #{matchedCount},
            completed_at = #{completedAt}
        WHERE id = #{id}
    </update>

    <select id="findByDocumentSrl" resultMap="adminKeywordMatchJobResultMap">
        SELECT
            id,
            document_srl,
            match_status,
            matched_count,
            created_at,
            completed_at
        FROM admin_keyword_match_job
        WHERE document_srl = #{documentSrl}
        ORDER BY created_at DESC, id DESC
    </select>

    <select id="findById" resultMap="adminKeywordMatchJobResultMap">
        SELECT
            id,
            document_srl,
            match_status,
            matched_count,
            created_at,
            completed_at
        FROM admin_keyword_match_job
        WHERE id = #{id}
    </select>
</mapper>
